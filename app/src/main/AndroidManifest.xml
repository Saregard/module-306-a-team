<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="se.linerotech.quizkampen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizKampen">
        <activity
            android:name=".Activitys.GameActivity"
            android:exported="true" />
        <activity android:name=".Activitys.ResultActivity" />
        <activity android:name=".Activitys.CreateAccount" />
        <activity android:name=".Activitys.ProfilePageActivity" />
        <activity android:name=".Activitys.LoginPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>